version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: store_postgres
    restart: always
    environment:
      POSTGRES_DB: app_news
      POSTGRES_USER: pc
      POSTGRES_PASSWORD: pc
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U django_user -d django_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: store_pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: pc@example.com
      PGADMIN_DEFAULT_PASSWORD: pc
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    depends_on:
      - postgres

volumes:
  postgres_data:
  pgadmin_data:


